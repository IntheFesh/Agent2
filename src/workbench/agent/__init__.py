"""Application-layer LangGraph agent.

NOTE (rule R3): this layer has never been benchmarked. Documentation describes mechanisms
only (approval gates, termination guards, memory gates) — never effectiveness.
"""
